#
# 로그함수
#

import matplotlib.pyplot as plt
import numpy as np

x = np.arange(1, 35.1, 0.1)
base = 1/2
y = np.log(x) / np.log(base) 
plt.plot(x, y, label='y=log1/2 x')

base = 2
y = np.log(x) / np.log(base)
plt.plot(x, y, label='y=log2 x')
plt.legend()
plt.show()
